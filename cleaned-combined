psychic-llama
=============
visualization of nyc innovation space
import nltk, itertools
data = itertools.chain.from_iterable([line.split() for line in open('./sinai/cleaned-combined','rb').read().splitlines()])
for word in data:
	print wordmkdir ./${1}
cd $1
cat ${1}-labs | while read line
do
	ifs='/' read -a array<<< $line
done
cat $(ls) > combined 
